version: '3.8'

services:
  game:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./configs:/root/configs
    environment:
      - DISPLAY=${DISPLAY}
    network_mode: host
    restart: unless-stopped
